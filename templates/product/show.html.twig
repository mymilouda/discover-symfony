{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 text-center">
            <h1>{{ product.name }}</h1>
            <h3>{{ product.price }} €</h3>
        </div>
        <div class="col-md-6">
            <p>{{ product.description }}</p>
        </div>
    </div>

    <div class="text-center">
        <a href="{{ path('app_product_order', { 'slug': product.slug }) }}" class="btn btn-info">Commander le produit</a>
    </div>
{% endblock %}
